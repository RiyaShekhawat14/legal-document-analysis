Place your fine-tuned legal model here.

Expected path:
- `backend/models/legal_lora_model/`

Supported layouts:
- A full Hugging Face model directory
- A PEFT/LoRA adapter directory containing `adapter_config.json`

If this is a LoRA adapter, also include the tokenizer files in the same folder.
The app will automatically try to load it for the AI assistant chat flow.
